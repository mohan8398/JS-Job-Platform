# DevHunter - Job Finder for MERN Developers

A clean, modern job finding platform built with React, Node.js, Express, and MongoDB.

## Features
- ✨ Clean, premium design with glassmorphism
- 🔍 Job search by title
- 🎯 Filter by tech stack (Node.js, React, MERN)
- 📱 Fully responsive layout
- ⚡ Fast and smooth animations with Framer Motion

## Tech Stack
- **Frontend:** React (Vite), Framer Motion, Lucide React, Axios
- **Backend:** Node.js, Express, MongoDB (Mongoose)
- **Styling:** Vanilla CSS (Modern Design System)

## Getting Started

### Prerequisites
- Node.js installed
- MongoDB installed and running locally

### Running the Backend
1. Open a terminal in the `server` directory
2. Run `npm install` (if not already done)
3. Run `node index.js`
   - The server will start on port 5000
   - It will automatically seed the database with initial jobs if empty

### Running the Frontend
1. Open another terminal in the `client` directory
2. Run `npm install` (if not already done)
3. Run `npm run dev`
4. Open the provided Vite URL (usually `http://localhost:5173`)

## Project Structure
- `/client`: React frontend
- `/server`: Express backend & API
